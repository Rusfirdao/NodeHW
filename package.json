{
    "scripts": {
        "test": "jest --ci --coverage && codecov"
    },
    "jest": {
        "coverageDirectory": "./coverage/",
        "collectCoverage": true
    },
    "devDependencies": {
        "jest": "^26.6.3"
    }
}
